# 最近改动报告：全量推理、纠错与社交链路优化

- 报告日期：2026-07-17
- 项目：`generative_agents`
- 状态：改动已在本地完成并通过针对性验证，当前尚未提交或推送
- 重点范围：全量决策提示词、纠错能力、策略字段、社交决策保护、对话与反思耗时

## 1. 改造背景

本轮改造的目标，是在不削弱 LLM 自主判断能力的前提下，降低全量推理的输入规模和附属调用成本，并修复实跑中暴露出的决策覆盖问题。

核心约束如下：

1. 不由程序过滤 LLM 可以选择的资源类型或动作类型。
2. 保留无效选择进入 LLM 的可能性，以继续观察它是否具有识错和纠错能力。
3. 程序可以压缩、去重和结构化事实，但不能替 LLM 做策略选择。
4. 保留上一步结果、经验、动机、资源利用、长期计划、其他 NPC 和后续动作落地等关键信息。
5. 保留“更有心机、更戏剧化、更长期主义”的决策空间，不能把提示词优化成只追求即时生存的短视决策器。

## 2. 最近一次实跑暴露的问题

最近分析的运行编号为：

```text
sim_20260717_210556
```

日志运行至约第 263 步后停止，检查时已无对应进程，日志中没有发现明确的 Python 崩溃栈。

### 2.1 全量决策提示词仍然偏长

实跑统计显示：

- 完整提示词 API 输入平均约 `21,308` 字符。
- 全量策略推理耗时中位数约 `9.26` 秒。
- 全量策略推理 P90 约 `19.37` 秒。
- 第 10 步之后，完整决策流水线中位数约 `12.45` 秒。
- 其中计划阶段中位数约 `9.40` 秒。

一个代表性提示词约为 `21,298` 字符，其中：

| 区域 | 字符数（约） |
| --- | ---: |
| 当前状态 | 12,464 |
| 已观察资源 | 5,004 |
| 世界资源目录 | 1,978 |
| 其他 NPC | 1,273 |
| 完整动作目录 | 4,561 |

资源信息存在大量重复，包括同类资源的重复描述、地址重复、库存重复和同一社会事件在不同对象上的重复出现。

### 2.2 长期策略字段没有稳定落地

该轮共观察到约 30 次决策：

- `strategic_intent`：23/30 有内容。
- `risk`：27/30 有内容。
- `expected_followup`：仅 3/30 有内容。

这说明模型虽然会表达当前意图和风险，但经常没有明确说明“完成当前动作后准备怎样继续”，不利于形成连续的长期计划。

### 2.3 纠错仍会重复犯同类错误

纠错流程中约有：

- 4 次成功修正。
- 1 次耗尽纠错尝试。
- 第 89 步出现重复选择自己作为人物动作目标的问题。

原纠错上下文没有足够明确地告诉模型当前行动者的完整姓名，也没有将“不得以自己为人物目标”作为权威状态约束表达出来。

### 2.4 正确的显式决策被同一步社交事件覆盖

第 262 步出现代表性问题：

1. Maria 的 LLM 决策为使用游戏机进行 `Recreate`。
2. 决策已正确翻译并找到执行目标。
3. 随后同一步内，一个来源为 `social_trigger`、原因为 `missing_schedule` 的社交反应覆盖了该决策。
4. Maria 最终转而执行聊天，而不是执行刚刚生成的显式决策。

这会降低 LLM 决策的实际控制力，也会使日志看起来像是模型“说一套、做一套”。

### 2.5 对话与反思产生过多附属 LLM 调用

完成一轮双人对话后，系统会为双方生成多类记忆元数据、反思、规划和关系更新。部分事实型元数据也由 LLM 生成，同时旧反思链路可能再次处理同一段聊天，形成重复调用。

极端慢步约为 `104–119` 秒，主要耗时集中在反思和执行阶段，而不是全量决策提示词本身。因此，仅压缩决策提示词不足以解决整体吞吐问题。

## 3. 已完成的改造

## 3.1 已观察资源上下文压缩

在 `run_gpt_prompt.py` 中新增或调整了资源事实抽取与紧凑上下文生成逻辑：

- 按标准化资源名称聚合同类资源。
- 保留所有资源类型，不删除候选资源。
- 同类资源只保留一个具有代表性的地址。
- 合并库存和可供性事实，避免逐对象重复描述。
- 仅保留与该资源确实相关的活动事件。
- 事件只有在目标或主体与资源匹配时才附加到资源条目。
- 完整决策和纠错提示词均不设置资源条目数量上限。

这属于信息去重和等价压缩，不是选项过滤。LLM 仍能看到完整的资源类型集合，也仍可能选择错误资源，从而保留对其纠错能力的检验。

合成样本对比结果：

| 指标 | 改造前 | 改造后 |
| --- | ---: | ---: |
| `ObservedResources` 字符数 | 约 6,635 | 约 1,193 |
| 压缩比例 | — | 约 82% |
| 保留资源类型数 | 31 | 31 |

完整动作目录仍然保留：

- 全量动作目录约 `4,532` 字符。
- 纠错动作目录约 `2,064` 字符。

## 3.2 世界资源静态说明压缩

在 `plan.py` 中，将世界资源能力说明改为更紧凑的结构，例如：

```text
apple tree[satiety]:可获取食物
```

该格式保留：

- 资源名称。
- 可影响的需求维度。
- 资源的主要用途。

同时去除了重复自然语言模板，减少提示词中反复出现的解释性句子。

## 3.3 社会事件去重

在 `plan.py` 中增加合作/社交事件去重：

- 同一社会事件即使挂载在多个对象上，也只进入上下文一次。
- 对象状态采集阶段同步去重重复事件。
- 不删除不同事件，也不替模型判断事件的重要性。

这能减少场景中多人、多对象互动时的上下文膨胀。

## 3.4 强化纠错提示词的权威约束

在完整决策和纠错链路中增加：

- 当前行动者的完整姓名。
- `self_target_forbidden=true` 权威状态。
- 明确说明人物类动作不得将行动者自己作为目标。
- 完整提示词与纠错提示词使用一致的自指约束。

纠错不是由程序直接替换目标。程序仍将错误、约束和完整可选动作交还给 LLM，由 LLM 自己生成修正版，因此纠错能力仍然可观测。

## 3.5 强化长期策略输出契约

完整决策模板现在要求模型输出：

- `strategic_intent`：当前动作服务于什么更大目标。
- `expected_followup`：当前动作完成后，优先推进什么后续动作。
- `risk`：可能导致计划失败、暴露、关系恶化或资源浪费的风险。

校验规则调整为：

- `risk` 必须始终非空。
- 非危急生理动机下，`strategic_intent` 必须非空。
- 非危急生理动机下，`expected_followup` 必须非空。
- 只有严重饥饿、口渴、疲劳等紧急状态，才允许后续计划为空。

这不会强制模型选择某个长期目标，只要求它把自己的策略链条表达完整，以便后续执行和日志审计。

## 3.6 改进校验错误的可观测性

在 `gpt_structure.py` 中，结构校验器现在可以返回：

```text
(是否通过, 具体错误列表)
```

安全重试日志会记录：

- 原始响应预览。
- 解析结果预览。
- 具体 `validation_errors`。
- 重试次数和最终结果。

因此下一轮遇到失败时，可以区分：

- JSON 或结构解析失败。
- 缺少必填策略字段。
- 自指目标。
- 非法动作或非法对象。
- 后续计划为空。
- 其他语义约束失败。

## 3.7 保护同一步内的新鲜显式决策

在 `plan.py` 中增加 `_has_fresh_explicit_decision()` 守卫。

当 NPC 在当前模拟时间已经产生并成功翻译一个 `decision_translation` 动作时：

- 同一步内的自动社交反应不能覆盖该动作。
- 守卫同时检查社交发起者和目标方。
- 被延迟的社交反应会记录日志事件：

```text
social_reaction_deferred
reason=fresh_explicit_decision
```

这个保护只针对“当前步刚刚生成的显式决策”，不会永久阻止 NPC 社交，也不会阻止后续步骤重新评估对话邀请。

## 3.8 降低对话与反思链路的调用成本

在 `chat_skill.py` 和 `reflect.py` 中完成以下调整：

1. 单次对话最大轮数从 8 轮降至 6 轮。
2. 对话后的计划、备忘和关系性内容仍由 LLM 生成。
3. 记忆事件的主谓宾、谓词和重要性等事实型元数据改由确定性结构生成。
4. 对话完成后记录 `chat_reflection_completed_fingerprint`。
5. 旧反思模块检测到相同聊天指纹已经处理时，跳过重复反思。

对一次完成的双人对话，理论上最多可减少约 20 次附属 LLM 调用，包括：

- 双方共约 8 次记忆三元组和重要性元数据调用。
- 最多约 12 次旧反思链路的重复调用。

仍然保留的高价值 LLM 工作包括：

- 对话总结。
- 八卦或可传播信息抽取。
- 计划与备忘生成。
- 关系更新。
- 经验记录。

## 4. 对 LLM 自主性与纠错实验的影响

本轮改造明确区分了“数据整理”和“替模型决策”。

程序负责：

- 合并完全重复的资源事实。
- 压缩等价描述。
- 去重同一社会事件。
- 校验结构与明确规则。
- 将错误原因反馈给模型。
- 防止调度层意外覆盖刚生成的显式决策。

程序不负责：

- 删除看似无用的资源候选。
- 根据需求替模型筛选动作。
- 自动把错误目标替换成正确目标。
- 直接指定长期计划。
- 强制某种情绪、阴谋或社交选择。

因此，仍然可以通过日志检查 LLM 是否会：

- 主动排除无关资源。
- 识别自身不能成为人物动作目标。
- 根据失败反馈修正动作。
- 利用人物关系和资源形成连续策略。
- 在即时需求与长期野心之间进行权衡。

## 5. 日志与下一轮验证指标

下一轮建议重点统计以下指标。

### 5.1 提示词规模

- 全量提示词总字符数的 P50、P90 和最大值。
- `ObservedResources` 的字符数和资源类型保留数。
- 当前状态、其他人物、动作目录各自占比。
- 资源数量增长时，提示词是否仍近似线性增长。

### 5.2 决策质量

- `strategic_intent` 非空率。
- `expected_followup` 非空率。
- `risk` 非空率。
- 后续一步是否与上一决策的 `expected_followup` 一致。
- 长期计划是否被环境变化合理调整，而不是机械重复。

### 5.3 纠错能力

- 首次输出有效率。
- 纠错成功率。
- 平均纠错次数。
- 同类错误在一次纠错中是否重复出现。
- 自指目标错误是否消失。
- 模型是否能根据精确 `validation_errors` 修复，而不是随机换动作。

### 5.4 决策执行一致性

- LLM 原始决策、翻译动作和最终执行动作是否一致。
- `social_reaction_deferred` 的出现次数。
- 延迟社交是否在后续步骤得到合理处理。
- 是否仍存在 `decision_translation` 被 `missing_schedule` 或其他自动调度覆盖。

### 5.5 性能

- 全量决策调用的 P50/P90。
- 单步总耗时的 P50/P90/最大值。
- `plan`、`execute`、`reflect` 分阶段耗时。
- 每次聊天产生的 LLM 调用数。
- 是否仍出现超过 60 秒或 100 秒的极端慢步。

## 6. 测试与检查结果

已完成的验证包括：

- 变更模块 Python 编译检查通过。
- 资源上下文和对象能力测试通过。
- 完整决策与纠错流水线测试通过。
- GPT 结构校验日志测试通过。
- 聊天证据与重复反思守卫测试通过。
- 技能请求路由测试通过。
- 决策提示词追踪、阶段 1 人设和结构化意图测试通过。
- 动作翻译收敛、决策约束和计划守卫测试通过。
- 社交触发与新鲜显式决策保护测试通过。
- 对话轮数限制和对话后反思测试通过。
- 聊天、社交与人物动作映射的针对性测试通过。

本轮相关测试累计约 150 项以上通过。

由于修改了 `ChatSkillPack`，还执行了 NPC 动作提示词同步审计。审计结果为“已闭环”，确认：

- 技能类实现完整。
- 技能已注册并包含别名。
- 动作标准化映射存在。
- 通信意图和 persona 路由存在。
- Socialize 结构和模板存在。
- 阶段 1 人物上下文能表达该动作。
- 对应测试已覆盖。

## 7. 已知问题与边界

当前工作树中还存在一些与本轮改造无关的既有修改和测试问题，本轮没有擅自清理：

- 部分旧聊天状态测试引用了缺失的 `log_social_decision`、`log_social_dialogue`。
- 个别测试夹具与 `action_outcome_history` 当前结构不一致。
- 一个旧动作映射测试仍预期 `leisure`，当前实现返回 `restore_mood`。
- 部分决策稳定性测试与当前 `BaseSkill` 接口不一致。
- 一个旧聊天状态文本仍预期 `pressure=low_mood`。
- 部分直接测试环境缺少 `openai` 或 `numpy`；使用项目自带运行环境或桩模块后，相关目标测试可通过。
- 全局 `git diff --check` 会发现无关旧文件 `test/test_clear_runtime_logs.py` 的尾随空格；本轮涉及文件的检查通过。

这些问题不影响本报告所述改造的针对性验证，但在执行全仓测试时仍会造成噪声。

## 8. 主要涉及文件

核心实现：

```text
reverie/backend_server/persona/prompt_template/run_gpt_prompt.py
reverie/backend_server/persona/prompt_template/gpt_structure.py
reverie/backend_server/persona/cognitive_modules/plan.py
reverie/backend_server/persona/cognitive_modules/reflect.py
reverie/backend_server/persona/cognitive_modules/skill_packs/chat_skill.py
reverie/backend_server/persona/prompt_template/v2/joint_decision_v1.txt
reverie/backend_server/persona/prompt_template/v2/joint_decision_correction_v1.txt
```

相关测试：

```text
test/test_demand_thinking_memory_context.py
test/test_gpt_structure_logging.py
test/test_chat_state_progression.py
test/test_joint_decision_pipeline.py
test/test_reflect_chat_evidence_guard.py
test/test_object_affordances.py
test/test_chat_skill_guards.py
```

## 9. 当前结论

本轮改造没有通过程序筛掉“无效选项”，而是压缩重复事实、增强规则反馈和保护 LLM 已经作出的显式决策。因此，它兼顾了两个目标：

1. 降低全量推理和聊天反思的耗时。
2. 继续真实检验 LLM 的选择、策略延续和纠错能力。

从静态测试和合成数据看，资源上下文压缩已明显生效；从架构上看，同一步社交覆盖和重复聊天反思也已得到针对性保护。最终效果仍应通过下一轮 500 步以上实跑确认，尤其要观察提示词规模、极端慢步、后续计划落地率和纠错成功率。

